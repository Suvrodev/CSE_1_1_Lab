#include<stdio.h>
main()
{
    int a,age,y,i,t,x;
    scanf("%d %d %d",&a,&age,&y);
    if(age<18)

    {
        printf("%d is a possitive number",x);
    }
else
    {
        printf("%d is a Negative number",x);
    }
}
